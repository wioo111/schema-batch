# 示例目录

这里用于存放开源仓库可直接运行的最小样例。

当前建议至少包含：

- 示例输入表格
- 对应任务模板
- 最小运行说明

当前已放入：

- `minimal-comments.csv`
  - 一个最小评论分析输入样例

建议使用方式：

1. 直接在桌面工作台中导入该文件
2. 选择 `templates/comment-analysis.template.json`
3. 填入自己的模型配置后执行
